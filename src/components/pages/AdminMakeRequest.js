import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminMakeRequest = () => (
  <Box sx={{ p: 4 }}>
    <Typography variant="h4" align="center" sx={{ mt: 4 }}>
      Make Request Page
    </Typography>
  </Box>
);

export default AdminMakeRequest; 