import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminViewAllRequest = () => (
  <Box sx={{ p: 4 }}>
    <Typography variant="h4" align="center" sx={{ mt: 4 }}>
      View All Request Page
    </Typography>
  </Box>
);

export default AdminViewAllRequest; 