import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminServiceCost = () => (
  <Box sx={{ p: 4 }}>
    <Typography variant="h4" align="center" sx={{ mt: 4 }}>
      Service Cost Page
    </Typography>
  </Box>
);

export default AdminServiceCost; 